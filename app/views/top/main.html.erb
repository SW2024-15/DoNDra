<!DOCTYPE html>
<html>
<head>
  <title>Main Page</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      margin: 0;
      padding: 0;
      height: 100vh; /* 画面全体の高さを占める */
      display: flex;
      justify-content: center; /* 横方向に中央揃え */
      align-items: center; /* 縦方向に中央揃え */
      flex-direction: column; /* 縦に並べる */
    }
    
    .button-container {
      display: flex;
      justify-content: center; /* 横方向に中央揃え */
      gap: 20px; /* ボタンの間にスペースを追加 */
    }
    
    .button {
      display: inline-block;
      margin: 10px;
      padding: 15px 25px;
      font-size: 16px;
      text-decoration: none;
      color: #fff;
      background-color: #00bfff;
      border-radius: 5px;
      border: none;
      cursor: pointer;
    }
    .button:hover {
      background-color: #0056b3;
    }
    
    h1 {
      margin-bottom: 100px;  /* h1の下に空白を追加 */
      font-size: 48px;  /* 文字サイズを大きく設定 */
      font-weight: bold; /* 文字を太く設定 */
    }
    
    /* リセットボタンのスタイル */
    .reset-button-container {
      margin-top: 20px;
    }

    .reset-button-description {
      font-size: 18px;
      color: #333;
      margin-bottom: 15px;
      font-weight: bold;
      margin-top: 80px;  /* スペース調整 */
    }

    .reset-button {
      background-color: #ffffff;  /* 背景色は白 */
      color: #000000;
      font-size: 16px;
      padding: 10px 20px;
      cursor: pointer;
      border: 4px solid #f0e68c;  /* 内側の枠線 */
      border-radius: 5px;
      box-shadow: 0 0 0 3px #f0e68c, 0 0 0 6px rgba(128, 0, 0, 0.2);  /* 2重枠線 */
    }

    .reset-button:hover {
      background-color: #febe00;  /* ホバー時に背景色を変更 */
      color: #fff;  /* 文字色を白に変更 */
      border-color: #febe00;  /* 枠線色をオレンジ色に変更 */
      box-shadow: 0 0 0 3px #febe00, 0 0 0 6px rgba(255, 127, 0, 0.2);  /* 2重枠線の色をオレンジ色に変更 */
    }
  </style>
</head>
<body>
  <h1>DoNDra</h1>
  
  <div class="button-container">
    <a href="<%= questions_path %>" class="button">問題に進む</a>
    <a href="<%= incorrect_user_answers_path %>" class="button">間違えた問題リスト</a>
  </div>
  
  <!-- リセットボタンの説明とボタン -->
  <div class="reset-button-container">
    <p class="reset-button-description">以下のボタンで、解答データは全てリセットされます。</p>
    <%= button_to 'リセット', reset_answers_path, method: :delete, class: 'button reset-button', data: { confirm: '解答データを削除してもよろしいですか？' } %>
  </div>
  
  <p>
    <%= link_to "ログアウト", top_logout_path %>
  </p>
</html>
